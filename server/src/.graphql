type User {
	id: Int!
	userName: String!
}

type Message {
	id: Int!
	senderId: Int!
	timestamp: Int!
	content: String!
}

type Query {
	messages(timestamp: Int!): [Message]
}

type Mutation {
	addMessage(senderId: Int!, content: String!): Message
}
